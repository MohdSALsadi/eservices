@{
    Layout = null;
}
@{
    Layout = null;
}

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f5f5f5;
            font-family: 'Roboto', sans-serif;
        }

        .container {
            margin-top: 50px;
        }

        .card {
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .card-body {
            background-color: #ffffff;
        }

        .thankyou-title {
            color: #28a745;
            font-weight: 700;
        }

        .thankyou-message {
            color: #333333;
        }

        .footer {
            margin-top: 20px;
            color: #777777;
            font-size: 12px;
            text-align: center;
        }

        .brand-logo {
            width: 15%;
            height: 15%;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body text-center">
                        <img src="/images/logo/logoRno.png" alt="branding logo" class="brand-logo">
                        <h2 class="thankyou-title mb-4">Thank You!</h2>
                        <p class="thankyou-message lead">Your registration request has been received</p>
                        <p class="thankyou-message">Our team will carefully review your request and get back to you soon.</p>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <footer class="footer">
                    <p>&copy; @DateTime.Now.ToString("MMMM dd, yyyy")</p>
                </footer>
            </div>
        </div>
        <div class="">
            <p class="text-center m-0"><a href="@Url.Action("login","account")" class="card-link btn btn-success">Register - Login</a></p>
        </div>
    </div>
</body>
</html>
